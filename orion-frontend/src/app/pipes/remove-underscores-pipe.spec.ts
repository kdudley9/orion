import { RemoveUnderscoresPipe } from './remove-underscores-pipe';

describe('RemoveUnderscoresPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveUnderscoresPipe();
    expect(pipe).toBeTruthy();
  });
});
